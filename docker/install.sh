service postgresql start

# psql -d paldb -U palette -f paldb.sql
# psql -d paldb -U palette -f domain.sql
# psql -d paldb -U palette -f environment.sql
# psql -d paldb -U palette -f roles.sql
# psql -d paldb -U palette -f users.sql
