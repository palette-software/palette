
# Automatically added by dh_python2:
if which pyclean >/dev/null 2>&1; then
	pyclean -p controller 
else
	dpkg -L controller | grep \.py$ | while read file
	do
		rm -f "${file}"[co] >/dev/null
  	done
fi

# End automatically added section
